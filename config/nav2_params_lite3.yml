# amcl:
#   ros__parameters:
#     use_sim_time: false
#     tf_broadcast: true
#     scan_topic: /scanner/scan
#     min_particles: 500
#     max_particles: 2000
#     odom_frame_id: "odom"
#     base_frame_id: "base_link"
#     global_frame_id: "map"
#     update_min_d: 0.05
#     update_min_a: 0.05
#     resample_interval: 1
#     set_initial_pose: true
#     initial_pose:
#       x: -1.33
#       y: 0.31
#       z: 0.0
#       yaw: 2.91

# map_server:
#   ros__parameters:
#     use_sim_time: false
#     yaml_filename: "/home/sunrise/zhengwu/map3floor/map3floor.yaml"

planner_server:
  ros__parameters:
    use_sim_time: false
    expected_planner_frequency: 5.0
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "nav2_navfn_planner/NavfnPlanner"
      tolerance: 0.5
      allow_unknown: false
    # MID-360 ICP odometry and projected scans are processed asynchronously and
    # currently arrive about 0.4-0.5 s behind wall time. Keep enough TF history
    # for costmap message filters to resolve the scan timestamp reliably.
    tf_buffer_duration: 10.0


controller_server:
  ros__parameters:
    use_sim_time: false
    tf_buffer_duration: 10.0
    controller_frequency: 5.0
    # min_x_velocity_threshold: 0.01
    # min_y_velocity_threshold: 0.01
    # min_theta_velocity_threshold: 0.01
    failure_tolerance: 0.5
    progress_checker_plugin: "progress_checker"
    goal_checker_plugins: ["general_goal_checker"] # "precise_goal_checker"
    controller_plugins: ["FollowPath"]

    # Progress checker parameters
    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.1
      movement_time_allowance: 30.0
    # Goal checker parameters
    general_goal_checker:
      stateful: True
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.15
      yaw_goal_tolerance: 0.15
      stateful: true
    # DWB parameters
    FollowPath:
      plugin: "dwb_core::DWBLocalPlanner"
      debug_trajectory_details: True
      min_vel_x: 0.0
      min_vel_y: 0.0
      max_vel_x: 0.5
      max_vel_y: 0.0
      max_vel_theta: 0.6
      # min_vel_theta: 0.05
      min_speed_xy: 0.0
      max_speed_xy: 0.4
      min_speed_theta: 0.05
      # Add high threshold velocity for turtlebot 3 issue.
      # https://github.com/ROBOTIS-GIT/turtlebot3_simulations/issues/75
      acc_lim_x: 0.5
      acc_lim_y: 0.0
      acc_lim_theta: 1.0
      decel_lim_x: -0.5
      decel_lim_y: -0.0
      decel_lim_theta: -1.0
      vx_samples: 20
      vy_samples: 5
      vtheta_samples: 120
      sim_time: 3.0
      linear_granularity: 0.05
      angular_granularity: 0.05
      transform_tolerance: 0.5
      xy_goal_tolerance: 0.15
      yaw_goal_tolerance: 0.15
      # trans_stopped_velocity: 0.15
      short_circuit_trajectory_evaluation: True
      stateful: True
      critics: ["RotateToGoal", "Oscillation", "BaseObstacle", "GoalAlign", "PathAlign", "PathDist", "GoalDist"]
      BaseObstacle.scale: 0.015
      PathAlign.scale: 1.0
      # PathAlign.forward_point_distance: 0.1
      # PathAlign.forward_preference: true
      GoalAlign.scale: 0.2
      # GoalAlign.forward_point_distance: 0.1
      # GoalAlign.forward_preference: true
      PathDist.scale: 0.5
      GoalDist.scale: 2.0
      RotateToGoal.xy_goal_tolerance: 0.20
      RotateToGoal.yaw_goal_tolerance: 0.20
      RotateToGoal.scale: 0.6
      RotateToGoal.slowing_factor: 1.0
      RotateToGoal.lookahead_time: 0.5


bt_navigator:
  ros__parameters:
    use_sim_time: false
    default_bt_xml_filename: "__ROBONIX_BT_XML__"
    tf_buffer_duration: 10.0

recoveries_server:
  ros__parameters:
    costmap_topic: global_costmap/costmap
    footprint_topic: global_costmap/published_footprint

local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 10.0
      # Updating stays fast for control, while RViz publication is throttled to
      # keep visualization overhead low on Jetson.
      publish_frequency: 2.0
      global_frame: odom
      robot_base_frame: base_link
      use_sim_time: false
      rolling_window: true
      width: 3
      height: 3
      resolution: 0.05
      # Lite3 Explorer physical size is 0.610 x 0.370 m. Four centimetres of
      # costmap padding per side gives a 0.690 x 0.450 m navigation envelope
      # without inflating the scan self-filter radius.
      footprint: "[ [0.305, 0.185], [0.305, -0.185], [-0.305, -0.185], [-0.305, 0.185] ]"
      footprint_padding: 0.04
      # robot_radius: 0.32

      plugins: ["obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: true
        observation_sources: scan
        scan:
          topic: /scanner/scan
          data_type: "LaserScan"
          max_obstacle_height: 2.0
          obstacle_min_range: 0.0
          obstacle_max_range: 2.5
          raytrace_min_range: 0.0
          raytrace_max_range: 3.0
          tf_filter_tolerance: 1.0
          inf_is_valid: true
          # ICP odometry and scan deskewing intentionally buffer the cloud until
          # odom -> base_link exists. Keep an accepted obstacle through the
          # resulting sub-second scan jitter instead of clearing it between scans.
          observation_persistence: 1.00
          expected_update_rate: 1.00
          marking: true
          clearing: true

      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        enabled: true
        inflation_radius: 0.55


global_costmap:
  global_costmap:
    ros__parameters:
      use_sim_time: false
      global_frame: map          # 通常是 map
      robot_base_frame: base_link
      # robot_radius: 0.32
      footprint: "[ [0.305, 0.185], [0.305, -0.185], [-0.305, -0.185], [-0.305, 0.185] ]"
      footprint_padding: 0.04
      update_frequency: 1.0      # 更新频率（Hz），比 local_costmap 低即可
      publish_frequency: 1.0     # 发布频率
      resolution: 0.05           # 分辨率，建议和地图一致

      rolling_window: false      # 🚫 不要滚动，通常设置为 false
      track_unknown_space: true  # 路径规划时是否考虑未知区域
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]

      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: true
        enabled: true

      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: true
        observation_sources: scan
        scan:
          topic: /scanner/scan
          data_type: "LaserScan"
          max_obstacle_height: 2.0
          obstacle_min_range: 0.0
          obstacle_max_range: 2.5
          raytrace_min_range: 0.0
          raytrace_max_range: 3.0
          tf_filter_tolerance: 1.0
          inf_is_valid: true
          observation_persistence: 1.00
          expected_update_rate: 1.00
          marking: true
          clearing: true

      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        enabled: true
        # Match the local controller's clearance envelope. A 2 m global
        # inflation radius can make nearly every corridor prohibitively costly.
        inflation_radius: 0.60
        cost_scaling_factor: 2.2


lifecycle_manager:
  ros__parameters:
    use_sim_time: false
    autostart: true
    node_names:
      - planner_server
      - controller_server
      - bt_navigator
      - recoveries_server
